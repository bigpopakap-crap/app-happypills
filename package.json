{
  "name": "@happypills/project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm-run-all install:client-web",
    "install:client-web": "cd client-web && yarn",
    "start": "npm-run-all start:client-web",
    "start:client-web": "cd client-web && yarn start",
    "build": "npm-run-all build:client-web",
    "build:client-web": "cd client-web && yarn build",
    "test": "npm-run-all test:client-web",
    "test:client-web": "cd client-web && yarn test",
    "lint": "npm-run-all lint:client-web",
    "lint:fix": "npm-run-all lint:client-web:fix",
    "lint:client-web": "cd client-web && yarn lint",
    "lint:client-web:fix": "cd client-web && yarn lint:fix"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
